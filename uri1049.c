#include<stdio.h>
int main()

{
    char s1[30],s2[30],s3[30];

    gets(s1);
    gets(s2);
    gets(s3);


    if(s1=="vertebrado" && s2=="ave" && s3== "carnivoro")
    {
        printf("Aguria");
    }
}

///vertebrado ave carnivoro;
